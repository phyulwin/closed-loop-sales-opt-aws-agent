import React from 'react'

export default function App() {
    return (
        <div className="app">
            <header className="header">
                <h1>React (HTML + CSS)</h1>
            </header>
            <main className="content">
                <p>This is a minimal Vite + React setup in the existing folder.</p>
                <button className="btn">A styled button</button>
            </main>
        </div>
    )
}
